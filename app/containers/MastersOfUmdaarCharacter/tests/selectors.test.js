// import { fromJS } from 'immutable';
// import { makeSelectMastersOfUmdaarCharacterDomain } from '../selectors';

// const selector = makeSelectMastersOfUmdaarCharacterDomain();

describe('makeSelectMastersOfUmdaarCharacterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
